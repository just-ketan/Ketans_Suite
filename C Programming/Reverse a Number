#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <math.h>

// reverse a number

int main(){
    int num,rev=0;
    scanf("%d",&num);
    while(num!=0){
        rev = rev*10 + num%10;
        num = num/10;
    }
    printf("Number : %d",rev);
    return 0;
}
